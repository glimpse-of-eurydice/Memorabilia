# Synthetic v0 run

This directory is a credential-free, deterministic export produced from the
synthetic fixture. It demonstrates the public run-bundle contract without a
live Codex trace, local absolute paths, credentials, or private model output.

Regenerate the ignored working copy with `npm run demo`. The tracked export is
frozen with apparatus version `0.1.0` and verified by the test suite.
